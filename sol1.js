function MaxNumber(num1, num2) {
    return num1 > num2 ? num1 : num2;
}

// Example:
const num1 = 10;
const num2 = 20;
const maxNumber = MaxNumber(num1, num2);
console.log("The maximum number is:", maxNumber); 
